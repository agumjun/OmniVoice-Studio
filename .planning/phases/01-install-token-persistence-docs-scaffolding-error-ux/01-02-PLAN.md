---
phase: 01-install-token-persistence-docs-scaffolding-error-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - docs/install/macos.md
  - docs/install/windows.md
  - docs/install/linux.md
  - docs/install/docker.md
  - docs/install/troubleshooting.md
  - docs/engines/cosyvoice.md
  - docs/features/diarization.md
  - docs/setup/huggingface-token.md
  - README.md
  - scripts/validate-install-docs.py
  - .github/workflows/ci.yml
  - backend/core/error_docs_map.py
  - backend/core/links.py
  - tests/backend/core/test_error_docs_map.py
  - frontend/src/utils/errorDocsMap.ts
  - frontend/src/utils/errorDocsMap.test.ts
  - frontend/src/components/ErrorBoundary.jsx
  - frontend/src/components/settings/ApiKeysPanel.jsx
  - frontend/src/components/settings/ApiKeysPanel.css
autonomous: true
requirements:
  - INST-02
  - INST-05
  - INST-06
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - AUTH-03

must_haves:
  truths:
    - "A user can read docs/install/macos.md (or windows/linux/docker) and reach a working app following only commands inside that file"
    - "A user who hits a Gatekeeper / AppImage white-screen / missing pkg_resources / HF 401 error sees an `Open docs for this error` button in the React UI that opens the right docs section in their default browser"
    - "When docs/install/*.md install commands diverge from scripts/desktop-prod.sh, CI fails"
    - "Settings → API Keys panel renders 3 source rows with `Active: <source>` badge wired to the Plan 01-01 resolver state endpoint"
    - "README no longer inlines the 600-line install section — it links to docs/install/*.md per OS"
    - "Every docs deeplink resolves to a real file (no 404s on the live repo)"
  artifacts:
    - path: "docs/install/macos.md"
      provides: "End-to-end macOS install + Gatekeeper xattr -cr workaround anchor"
      contains: "gatekeeper-quarantine"
    - path: "docs/install/windows.md"
      provides: "End-to-end Windows install + setx vs SetEnvironmentVariable note"
      contains: "SetEnvironmentVariable"
    - path: "docs/install/linux.md"
      provides: "End-to-end AppImage + .deb install + WebKit white-screen anchor"
      contains: "appimage-white-screen"
    - path: "docs/install/docker.md"
      provides: "Docker install + LAN access note (covered by Plan 01-03 frontend fix)"
    - path: "docs/install/troubleshooting.md"
      provides: "Top 10 install errors with cause/fix/issue links"
      min_lines: 80
    - path: "docs/engines/cosyvoice.md"
      provides: "CosyVoice install + troubleshooting (closes #55)"
    - path: "docs/features/diarization.md"
      provides: "Diarization HF gating + pyannote license acceptance"
    - path: "docs/setup/huggingface-token.md"
      provides: "HF token guide covering Settings → API Keys + env var + huggingface-cli paths"
    - path: "scripts/validate-install-docs.py"
      provides: "Docs drift CI gate — diffs validated code blocks against desktop-prod.sh"
      min_lines: 50
    - path: "backend/core/error_docs_map.py"
      provides: "Python error_class → docs URL map + DEFAULT_DOCS fallback"
      contains: "GATEKEEPER_QUARANTINE"
    - path: "backend/core/links.py"
      provides: "Single source of truth for project repo URL (read from pyproject.toml urls + Tauri config)"
    - path: "frontend/src/utils/errorDocsMap.ts"
      provides: "TS mirror of error_docs_map.py + openDocsFor(errorClass)"
    - path: "frontend/src/components/settings/ApiKeysPanel.jsx"
      provides: "Settings → API Keys panel UI (AUTH-03)"
      min_lines: 100
  key_links:
    - from: "README.md"
      to: "docs/install/*.md"
      via: "OS-specific install links replacing inlined steps"
      pattern: "docs/install/(macos|windows|linux|docker)\\.md"
    - from: ".github/workflows/ci.yml"
      to: "scripts/validate-install-docs.py"
      via: "CI step running the validator on every PR"
      pattern: "validate-install-docs\\.py"
    - from: "frontend/src/components/ErrorBoundary.jsx"
      to: "frontend/src/utils/errorDocsMap.ts"
      via: "openDocsFor(error.errorClass) called from the error toast button"
      pattern: "openDocsFor"
    - from: "frontend/src/components/settings/ApiKeysPanel.jsx"
      to: "backend/api/routers/settings.py (Plan 01-01)"
      via: "fetch('/api/settings/hf-token/state') and POST/DELETE"
      pattern: "/api/settings/hf-token"
    - from: "backend/core/error_docs_map.py"
      to: "backend/core/links.py"
      via: "URL templating with PROJECT_REPO_URL constant"
      pattern: "links\\.PROJECT_REPO_URL"

threat_model:
  - id: T-02-01
    category: Tampering
    component: frontend/src/utils/errorDocsMap.ts
    disposition: mitigate
    mitigation: "All deeplink URLs are hardcoded to point to github.com/<repo> resolved from links.PROJECT_REPO_URL; no user-controlled URL input; Tauri shell.open() opens user's browser (not in-app webview), so even a poisoned URL cannot inject into the app's DOM"
  - id: T-02-02
    category: Information Disclosure
    component: frontend/src/components/settings/ApiKeysPanel.jsx
    disposition: mitigate
    mitigation: "Panel never renders the full token — only masked `hf_…<last3>` from the resolver state endpoint; full token never crosses the IPC boundary on read, only on Save (POST)"
  - id: T-02-03
    category: Tampering
    component: scripts/validate-install-docs.py
    disposition: mitigate
    mitigation: "Validator runs in CI on every PR; if a contributor edits docs/install/*.md without updating desktop-prod.sh (or vice versa), CI fails before merge. Pitfall #4 normalization prevents flakes on prompt prefixes/trailing whitespace"
---

<objective>
Split the 600-line README install section into per-OS markdown files, ship the docs drift CI gate, build the error→docs deeplink map (Python + TS halves), wire the deeplink button into the React error toast/boundary, and ship the Settings → API Keys panel that consumes the Plan 01-01 resolver state endpoint.

Purpose: New users follow ONE OS-specific docs file end-to-end. When something breaks, the React error UI links them straight to the relevant docs section. The validator script prevents the docs from drifting out of sync with the real install path. The Settings panel makes the 3-source token cascade visible and editable.

Output: Docs tree + validator + CI step + error_docs_map (both languages) + Settings UI panel + ErrorBoundary deeplink wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-install-token-persistence-docs-scaffolding-error-ux/01-RESEARCH.md
@.planning/phases/01-install-token-persistence-docs-scaffolding-error-ux/01-01-SUMMARY.md

@CLAUDE.md
@README.md
@scripts/desktop-prod.sh
@.github/workflows/ci.yml
@frontend/src/components/ErrorBoundary.jsx
@pyproject.toml

<interfaces>
<!-- Plan 01-01 provides this resolver state API. Plan 01-02 (this plan) consumes it. -->

```
GET /api/settings/hf-token/state →
{
  "sources": [
    {"source": "app",    "set": bool, "masked": "hf_…3jw"|null, "whoami_user": str|null, "whoami_ok": bool},
    {"source": "env",    "set": bool, "masked": "hf_…3jw"|null, "whoami_user": str|null, "whoami_ok": bool},
    {"source": "hf-cli", "set": bool, "masked": "hf_…3jw"|null, "whoami_user": str|null, "whoami_ok": bool}
  ],
  "active": "app"|"env"|"hf-cli"|null
}

POST /api/settings/hf-token  {"token": "hf_..."}  → returns updated state (loopback-only)
DELETE /api/settings/hf-token?also_clear_hf_cli=false  → returns updated state (loopback-only)
```

<!-- Error class taxonomy this plan defines (4 classes per RESEARCH.md). -->
ErrorClass:
  - "GATEKEEPER_QUARANTINE"         → docs/install/macos.md#gatekeeper-quarantine
  - "APPIMAGE_WEBKIT_WHITESCREEN"   → docs/install/linux.md#appimage-white-screen-on-fedora-44--ubuntu-2404
  - "PKG_RESOURCES_MISSING"         → docs/install/troubleshooting.md#pkg_resources-missing
  - "HF_AUTH_FAILED"                → docs/setup/huggingface-token.md
  - <unknown>                       → docs/install/troubleshooting.md  (DEFAULT)
</interfaces>

<existing_state>
- `README.md` currently inlines ~600 lines of install instructions across macOS / Windows / Linux. This plan extracts them.
- `.github/workflows/ci.yml` exists with cross-platform matrix (Phase 0 work). Add a single step running the validator on Linux only (docs are platform-agnostic; validator doesn't need matrix).
- `scripts/desktop-prod.sh` is the canonical install/launch path. Validator must extract code blocks marked `<!-- validate -->` from `docs/install/*.md` and normalize-compare against `desktop-prod.sh` per Pitfall #4.
- Plan 01-01 ships `backend/api/routers/settings.py` with the 3 endpoints above. The Settings UI panel in this plan calls those endpoints via `fetch` (or via the existing `apiBase` helper if present; if not, hardcode `http://localhost:3900` for now and let Plan 01-03 centralize via `frontend/src/utils/apiBase.ts`).
- `frontend/src/components/ErrorBoundary.jsx` already exists per Plan-1 file listing (confirmed via `ls frontend/src/components/`). Patch it to call `openDocsFor` from the new errorDocsMap when an error has a known class.
- `frontend/src/components/settings/` directory does NOT exist yet — create it.
- Project repo URL: `pyproject.toml [project.urls].Repository = "https://github.com/k2-fsa/OmniVoice"` (upstream model) vs Tauri updater pointing to `github.com/debpalash/OmniVoice-Studio` (the desktop app fork). For docs deeplinks, the DESKTOP APP fork is correct. Plan 01-01 surfaces a `backend/core/links.py` constant; this plan reuses it and adds the equivalent TS-side constant.
</existing_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docs scaffolding — split README + write 5 new docs + validator + CI step</name>
  <files>docs/install/macos.md, docs/install/windows.md, docs/install/linux.md, docs/install/docker.md, docs/install/troubleshooting.md, docs/engines/cosyvoice.md, docs/features/diarization.md, docs/setup/huggingface-token.md, README.md, scripts/validate-install-docs.py, .github/workflows/ci.yml</files>
  <action>
    Step 1: Read `README.md` and `scripts/desktop-prod.sh` end-to-end to extract the canonical install commands per OS. Note the existing macOS Gatekeeper note (#54 docs may already be present from PR #62 — preserve and extend).

    Step 2: Create `docs/install/macos.md`. Sections (use these exact heading anchors so deeplinks work):
      - `## Prerequisites` — Python 3.11+, bun/node, Xcode CLI tools
      - `## Install` — `bun install`, `bun run desktop-prod` (wrap commands in fences marked `<!-- validate -->` so the script gates them)
      - `## Gatekeeper quarantine` (anchor: `gatekeeper-quarantine`) — explanation of unsigned .app + `xattr -cr "/Applications/OmniVoice Studio.app"` workaround + SHA-256 verification (links to GATE-05 release-notes section). Note: real notarization deferred to v2 (link to v2 tracking).
      - `## Apple Silicon vs Intel` — note about mlx-whisper/mlx-audio only on arm64
      - `## Troubleshooting` — link to `troubleshooting.md`
      INST-03 docs half lives in this file.

    Step 3: Create `docs/install/windows.md`. Sections:
      - `## Prerequisites` — Python 3.11+ (winget install Python.Python.3.11), Microsoft C++ Build Tools, bun/node
      - `## Install` — fenced `<!-- validate -->` install commands
      - `## HF_TOKEN persistence` — PowerShell `[Environment]::SetEnvironmentVariable("HF_TOKEN","hf_...","User")` is correct; `setx` is documented but NOT recommended (Pitfall: doesn't propagate to current shell, per CLAUDE.md "What NOT to Use"). Recommend Settings → API Keys panel as the primary path.
      - `## Triton / torch.compile OOM` (anchor: `torch-compile-oom`) — covers #65 via INST-12 (toggle in Settings → Performance ships in Phase 2 per REQUIREMENTS.md INST-13 — document the workaround now and reference the Phase 2 toggle as "coming"). Use `TORCH_COMPILE_DISABLE=1` env var as the v0.3 user-side workaround.
      - `## Troubleshooting` link.

    Step 4: Create `docs/install/linux.md`. Sections:
      - `## Prerequisites` — Python 3.11+ (apt or system), bun/node
      - `## Install` — fenced `<!-- validate -->` commands
      - `## AppImage white-screen on Fedora 44 / Ubuntu 24.04` (anchor: `appimage-white-screen-on-fedora-44--ubuntu-2404`) — explanation of WebKit 2.44/2.46 compositing bug + `WEBKIT_DISABLE_COMPOSITING_MODE=1` workaround. Note that Plan 01-03 ships a conditional AppRun launcher that sets this automatically on the broken range — but env-var fallback is documented in case the user runs from source.
      - `## .deb ffprobe conflict` (anchor: `deb-ffprobe-conflict`) — explains pre-v0.3 .deb overwrote `/usr/bin/ffprobe`; v0.3 .deb relocates it (covered by Plan 01-03). For users upgrading from older .deb, the postrm script handles cleanup.
      - `## Restricted networks (China / Russia)` — pointer to env vars `UV_PYTHON_INSTALL_MIRROR`, `UV_HTTP_TIMEOUT=120`, `UV_HTTP_RETRIES=5`, `UV_PYTHON_PREFERENCE=only-system` fallback. Note the full mirror cascade ships in Phase 3 (INST-07..11); this section documents the user-side env-var path for v0.3.
      INST-04 docs half lives in this file.

    Step 5: Create `docs/install/docker.md`. Sections:
      - `## Pull and run` (fenced `<!-- validate -->` if applicable, or unvalidated if container-only commands not present in desktop-prod.sh)
      - `## LAN access` — when running in Docker exposed on a LAN address, the frontend now uses `window.location.host` for the preview API base (Plan 01-03 ships the fix). Document the `VITE_OMNIVOICE_API` env-var override for explicit pinning.
      - `## Volume mounts` — `omnivoice_data/` and `~/.cache/huggingface/` mount recommendations

    Step 6: Create `docs/install/troubleshooting.md` covering the top 10 install errors (DOCS-01). Each entry: `### Cause: ...`, `### Fix: ...`, `### Linked issue: #XX`. Required entries (minimum):
      1. `pkg_resources` missing (anchor: `pkg_resources-missing`) → setuptools pin (closes #58)
      2. HF 401 / pyannote license not accepted → Settings → API Keys + accept license (closes #35)
      3. Gatekeeper quarantine on macOS → see `macos.md#gatekeeper-quarantine`
      4. AppImage white screen on Fedora 44 / Ubuntu 24.04 → see `linux.md#appimage-white-screen-on-fedora-44--ubuntu-2404`
      5. Windows Triton/torch.compile OOM → see `windows.md#torch-compile-oom`
      6. `uv venv` Python download fails (restricted network) → see `linux.md#restricted-networks-china--russia`
      7. .deb ffprobe path conflict on upgrade → see `linux.md#deb-ffprobe-conflict`
      8. Docker LAN access — media preview 404 → covered in `docker.md#lan-access`
      9. Apple Silicon mlx-whisper unavailable on Intel mac → fall back to faster-whisper
      10. IndexTTS clash with other engines → planned fix in Phase 2 (subprocess isolation); workaround: install only one of the conflicting engines per venv

    Step 7: Create `docs/engines/cosyvoice.md` (DOCS-03, closes #55). Sections: install path, common errors (model not found, license gating), HF token requirement, troubleshooting.

    Step 8: Create `docs/features/diarization.md` (DOCS-04, closes #35 sub-issue). Sections: what diarization does, pyannote/speaker-diarization-3.1 license acceptance flow (with screenshot reference to huggingface.co), HF token requirement (link to `huggingface-token.md`), fallback behavior (silence-gap heuristic per `dub_core.py` reason strings).

    Step 9: Create `docs/setup/huggingface-token.md` (DOCS-05). Sections:
      - `## Three sources` — explain the cascade (App → Env → HF CLI), point to Settings → API Keys as the recommended path
      - `## Setting via the app (recommended)` — Settings → API Keys panel screenshot reference + how the App-source token is stored (SQLite, encrypted at rest, machine-ID-derived key) and the known limitation: doesn't migrate across machines (Open Question #5 honest doc per Pitfall #5)
      - `## Setting via environment variable (power users)` — macOS zsh / Linux bash / Windows PowerShell one-liners verbatim from CLAUDE.md Capability 1 (zsh: `echo 'export HF_TOKEN=hf_xxx' >> ~/.zshrc && source ~/.zshrc`; bash: same with `.bashrc`; PowerShell: `[Environment]::SetEnvironmentVariable("HF_TOKEN","hf_xxx","User")`). Explicitly warn against `setx` (CLAUDE.md gotcha — doesn't propagate to current shell).
      - `## Setting via huggingface-cli` — `huggingface-cli login` writes to `~/.cache/huggingface/token`; OmniVoice reads via `huggingface_hub.get_token()`
      - `## Accepting model licenses` — pyannote, IndexTTS, Supertonic-3 links

    Step 10: Patch `README.md` — REMOVE the inlined install section (preserve the header) and REPLACE with a short routing block:
      ```
      ## Install
      - **macOS:** see [docs/install/macos.md](docs/install/macos.md)
      - **Windows:** see [docs/install/windows.md](docs/install/windows.md)
      - **Linux:** see [docs/install/linux.md](docs/install/linux.md)
      - **Docker:** see [docs/install/docker.md](docs/install/docker.md)

      Stuck? See [docs/install/troubleshooting.md](docs/install/troubleshooting.md).
      ```
      Implements INST-02.

    Step 11: Templated version badges (INST-05). Replace any hardcoded `v0.2.x` references in README badges with the dynamic shields.io pattern that reads from the latest GitHub release:
      `https://img.shields.io/github/v/release/<owner>/<repo>?label=download`
      using the project repo URL from `backend/core/links.py` resolution (or its TS equivalent for the README's purpose, just hardcode the URL — links.py is consumed at runtime, README is rendered by GitHub).

    Step 12: Create `scripts/validate-install-docs.py` (INST-06). Implementation per RESEARCH.md skeleton with Pitfall #4 normalization:
      - Extract all fenced code blocks tagged `<!-- validate -->` immediately before the fence
      - Normalize: strip `$ ` / `>>> ` prompt prefixes, rstrip trailing whitespace, normalize CRLF→LF, skip blank and comment-only lines
      - Build a set of normalized lines from `scripts/desktop-prod.sh` (similarly normalized)
      - For each block line not present in the canonical set, append to errors
      - Exit 1 with the error list on stderr if errors; print `OK — N install docs validated` on success
      - Support a `<!-- validate: skip -->` marker for examples that intentionally diverge

    Step 13: Wire into `.github/workflows/ci.yml`. Add a new job `validate-docs` (or step in an existing job) that:
      - Runs on `ubuntu-latest` only (docs platform-agnostic)
      - Sets up Python 3.11 via `actions/setup-python@v5`
      - Runs `python scripts/validate-install-docs.py`
      - Triggers on `pull_request` and `push` to `main`
      Per RESEARCH.md Validation Architecture.

    Decision: docs files for Linux/Windows/macOS that exercise install paths NOT covered by `desktop-prod.sh` (e.g. AppImage download via curl from a release URL) should NOT be `<!-- validate -->` tagged — only the parts that match `desktop-prod.sh` get validated. The validator catches drift in the OmniVoice-provided install path; it does NOT enforce that arbitrary docs commands match anything.
  </action>
  <verify>
    <automated>python scripts/validate-install-docs.py && test -f docs/install/macos.md && test -f docs/install/windows.md && test -f docs/install/linux.md && test -f docs/install/docker.md && test -f docs/install/troubleshooting.md && test -f docs/engines/cosyvoice.md && test -f docs/features/diarization.md && test -f docs/setup/huggingface-token.md && grep -q "docs/install/macos.md" README.md && grep -v '^#' README.md | grep -qE "^\s*$|^\s*-" </automated>
  </verify>
  <done>
    All 8 docs files exist, validator runs clean on the initial docs set, CI workflow includes the validate-docs step, README routes users to per-OS files (no inlined 600-line install section), and `python scripts/validate-install-docs.py` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Error → docs deeplink map (Python + TS halves)</name>
  <files>backend/core/links.py, backend/core/error_docs_map.py, tests/backend/core/test_error_docs_map.py, frontend/src/utils/errorDocsMap.ts, frontend/src/utils/errorDocsMap.test.ts</files>
  <behavior>
    Python (`test_error_docs_map.py`):
      - `test_known_class_returns_url`: `lookup("GATEKEEPER_QUARANTINE")` returns the macos.md anchor URL.
      - `test_unknown_class_returns_default`: `lookup("NEVER_HEARD_OF_IT")` returns `DEFAULT_DOCS`.
      - `test_all_urls_resolve_to_repo`: every URL in `ERROR_DOCS` and `DEFAULT_DOCS` starts with `links.PROJECT_REPO_URL`.
      - `test_all_keys_match_taxonomy`: keys are exactly the 4-class taxonomy (Gatekeeper, AppImage WebKit, pkg_resources, HF auth).
    TypeScript (`errorDocsMap.test.ts` — vitest):
      - `test_openDocsFor_known_class_calls_shell_open`: mocks `@tauri-apps/plugin-shell.open`, calls `openDocsFor("HF_AUTH_FAILED")`, asserts mock was called with the huggingface-token.md URL.
      - `test_openDocsFor_unknown_class_falls_back_to_default`: calls `openDocsFor("BOGUS")`, asserts mock was called with the default URL.
      - `test_keys_match_python_map`: a sentinel test that lists the 4 keys and asserts they match the Python map's keys verbatim — guards against drift between the two language halves.
  </behavior>
  <action>
    Step 1: Create `backend/core/links.py` (single source of truth for project repo URL). Logic:
      - Read `pyproject.toml [project.urls].Repository` via `tomllib` (stdlib in 3.11+).
      - Read `frontend/src-tauri/tauri.conf.json` `plugins.updater.endpoints[0]` and extract `github.com/<owner>/<repo>` substring.
      - Prefer the Tauri config URL (it points to the desktop app fork — `github.com/debpalash/OmniVoice-Studio` — which is where docs deeplinks should resolve). Fall back to pyproject if Tauri config is unreadable.
      - Expose: `PROJECT_REPO_URL: str` (e.g. `https://github.com/debpalash/OmniVoice-Studio`), `PROJECT_REPO_BLOB_MAIN: str` (e.g. `<url>/blob/main`).
      - Cache the resolved value at import time.
      Resolves Open Question #3 from RESEARCH.md.

    Step 2: Create `backend/core/error_docs_map.py`:
      ```
      from backend.core.links import PROJECT_REPO_BLOB_MAIN as _BASE

      ERROR_DOCS = {
          "GATEKEEPER_QUARANTINE":      f"{_BASE}/docs/install/macos.md#gatekeeper-quarantine",
          "APPIMAGE_WEBKIT_WHITESCREEN":f"{_BASE}/docs/install/linux.md#appimage-white-screen-on-fedora-44--ubuntu-2404",
          "PKG_RESOURCES_MISSING":      f"{_BASE}/docs/install/troubleshooting.md#pkg_resources-missing",
          "HF_AUTH_FAILED":             f"{_BASE}/docs/setup/huggingface-token.md",
      }
      DEFAULT_DOCS = f"{_BASE}/docs/install/troubleshooting.md"

      def lookup(error_class: str | None) -> str:
          return ERROR_DOCS.get(error_class or "", DEFAULT_DOCS)
      ```

    Step 3: Write `tests/backend/core/test_error_docs_map.py` per `<behavior>`.

    Step 4: Create `frontend/src/utils/errorDocsMap.ts`. Mirror the Python map exactly. Read the project repo URL from a hardcoded constant matching `links.PROJECT_REPO_URL` (the TS side runs in the browser; it can't read `pyproject.toml`). Document the duplication: comment header `// MIRROR OF backend/core/error_docs_map.py — keep in sync. test_keys_match_python_map enforces this.`
      ```
      import { open } from "@tauri-apps/plugin-shell";

      const BASE = "https://github.com/debpalash/OmniVoice-Studio/blob/main";

      export const ERROR_DOCS: Record<string, string> = {
        GATEKEEPER_QUARANTINE: `${BASE}/docs/install/macos.md#gatekeeper-quarantine`,
        APPIMAGE_WEBKIT_WHITESCREEN: `${BASE}/docs/install/linux.md#appimage-white-screen-on-fedora-44--ubuntu-2404`,
        PKG_RESOURCES_MISSING: `${BASE}/docs/install/troubleshooting.md#pkg_resources-missing`,
        HF_AUTH_FAILED: `${BASE}/docs/setup/huggingface-token.md`,
      };
      export const DEFAULT_DOCS = `${BASE}/docs/install/troubleshooting.md`;

      export function openDocsFor(errorClass: string | null | undefined): Promise<void> {
        return open(ERROR_DOCS[errorClass ?? ""] ?? DEFAULT_DOCS);
      }
      ```

    Step 5: Write `frontend/src/utils/errorDocsMap.test.ts` per `<behavior>`. Use vitest if configured (check `package.json`); if vitest isn't configured, set it up minimally — add `"test": "vitest run"` script if absent, and a `vitest.config.ts` with `environment: 'jsdom'`. Mock `@tauri-apps/plugin-shell` via `vi.mock('@tauri-apps/plugin-shell', () => ({ open: vi.fn() }))`.

    Step 6: Confirm Plan 01-01's `backend/core/links.py` doesn't already exist (per Plan 01-01 output declaring this constant). If 01-01 created it, this task EXTENDS it (adds `PROJECT_REPO_BLOB_MAIN`). If 01-01 did not, this task CREATES it. The Plan 01-01 SUMMARY will tell us which.

    Threat T-02-01 mitigation: deeplink URLs are hardcoded; user input never reaches `open()`. Tauri shell.open opens external browser, not in-app webview.
  </action>
  <verify>
    <automated>uv run pytest tests/backend/core/test_error_docs_map.py -x -v && (cd frontend && bun test errorDocsMap)</automated>
  </verify>
  <done>
    Python map + tests green. TS map + tests green. The 4-key taxonomy matches between both maps. `links.PROJECT_REPO_URL` resolves to a valid GitHub URL that any deeplink can be appended to.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire deeplinks into ErrorBoundary + ship Settings → API Keys panel</name>
  <files>frontend/src/components/ErrorBoundary.jsx, frontend/src/components/settings/ApiKeysPanel.jsx, frontend/src/components/settings/ApiKeysPanel.css, frontend/src/components/settings/ApiKeysPanel.test.jsx</files>
  <behavior>
    `ErrorBoundary`:
      - When a child throws an Error with `errorClass` property (or matching a heuristic mapping known error message → class), the rendered fallback UI includes an "Open docs for this error" button.
      - Clicking the button calls `openDocsFor(error.errorClass)` from `errorDocsMap.ts`.
      - When no class matches, the button still renders pointing to DEFAULT_DOCS.
    `ApiKeysPanel`:
      - Renders 3 rows on mount, populated from `GET /api/settings/hf-token/state`.
      - Each row shows: source name (App / Env var / HF CLI), set/unset indicator, masked token (`hf_…3jw`) when set, whoami username + checkmark when whoami_ok, "Test now" button (calls `GET /api/settings/hf-token/state` again with a no-cache hint).
      - "Active: <source>" badge appears on the row matching `state.active`.
      - App row has Save + Clear buttons (POST/DELETE). Env and HF-CLI rows are read-only.
      - Clear button shows a confirm dialog with "Also clear ~/.cache/huggingface/token?" checkbox (default off).
      - Test asserts: panel renders 3 rows after mock fetch resolves; Save POST is called with the entered token; Clear DELETE is called; Active badge appears on the right row.
  </behavior>
  <action>
    Step 1: Read `frontend/src/components/ErrorBoundary.jsx`. Add an "Open docs for this error" button to the fallback render path. Import `openDocsFor` from `../utils/errorDocsMap`. The error may not carry an explicit `errorClass` — add a heuristic mapper in the component:
      - error.message contains "pkg_resources" → PKG_RESOURCES_MISSING
      - error.message contains "401" OR "HfHubHTTPError" → HF_AUTH_FAILED
      - error.message contains "WebKit" OR "white screen" → APPIMAGE_WEBKIT_WHITESCREEN
      - error.message contains "quarantine" OR "Gatekeeper" → GATEKEEPER_QUARANTINE
      - default → null (falls to DEFAULT_DOCS)
      Button text: "Open docs for this error" → calls `openDocsFor(matchedClass)`.

    Step 2: Create `frontend/src/components/settings/ApiKeysPanel.jsx`. Implementation per AUTH-03:
      - useState for sources (3 entries) + active + loading + saveDialog state
      - useEffect on mount: `fetch('/api/settings/hf-token/state').then(r => r.json()).then(setState)`
      - Render 3 source rows in a `<table>` or `<dl>` (CSS friendly). Each row: name, status icon, masked token, whoami badge, action buttons.
      - Active badge: small `<span className="active-badge">Active</span>` next to the source name on the row matching `state.active`.
      - Save flow (App row only): input + Save button → POST `/api/settings/hf-token` body `{token}` → on 200 refetch state. Pitfall #2: ensure the panel doesn't surface `add_to_git_credential` to the user (backend handles).
      - Clear flow (App row only): button → small dialog with checkbox "Also clear ~/.cache/huggingface/token?" → DELETE `/api/settings/hf-token?also_clear_hf_cli={bool}` → on 200 refetch.
      - "Test now" button per row: calls `GET /api/settings/hf-token/state` again (the resolver internally re-validates non-cached; via the `invalidate()` hook from Plan 01-01).

    Step 3: Create `frontend/src/components/settings/ApiKeysPanel.css`. Minimal styling matching the existing OmniVoice settings design (read `frontend/src/components/*.css` for the existing palette/spacing; the file `LogsFooter.css` and `KeyboardCheatsheet.css` are good references).

    Step 4: Mount the panel into the existing Settings page. Grep `frontend/src/` for a `SettingsPage` / `SettingsView` / similar — if it exists, add a new section "API Keys"; if it doesn't, create a stub `SettingsPage.jsx` that renders just `<ApiKeysPanel />` for now. The full Settings UI restructure is out of scope; this task only ships the API Keys panel.

    Step 5: Write `frontend/src/components/settings/ApiKeysPanel.test.jsx` per `<behavior>`. Use vitest + `@testing-library/react` + `vi.fn()` to mock `fetch`. Pre-resolve `fetch` with a synthetic response matching the state contract from `<interfaces>`.

    Threat T-02-02 mitigation: panel reads `masked` from the resolver state endpoint, never the full token. Save POST sends the full token from a user-typed input box (cleared on success) and the input value is never logged.

    Per AUTH-03 — backend half ships in Plan 01-01; this task ships the UI half.
  </action>
  <verify>
    <automated>(cd frontend && bun test ApiKeysPanel) && (cd frontend && bun test ErrorBoundary)</automated>
  </verify>
  <done>
    ErrorBoundary fallback renders a docs deeplink button that opens the right docs for known error classes. API Keys panel renders 3 source rows from the resolver state endpoint, Save/Clear/Test buttons round-trip correctly to the Plan 01-01 endpoints, and the Active badge displays on the right row. All component tests pass.
  </done>
</task>

</tasks>

<verification>
  After all three tasks:
  - `python scripts/validate-install-docs.py` exits 0
  - `uv run pytest tests/backend/core/test_error_docs_map.py -x` → green
  - `(cd frontend && bun test)` filtered to `errorDocsMap`, `ApiKeysPanel`, `ErrorBoundary` → green
  - Manual: open the running app, navigate to Settings → API Keys, see 3 rows
  - Manual: open https://github.com/debpalash/OmniVoice-Studio/blob/main/docs/install/macos.md#gatekeeper-quarantine in a browser — must 404-free (the anchor must resolve to a real heading in the committed file)
  - `grep -r "docs/install/" README.md | wc -l` → ≥ 4 (one link per OS file)
</verification>

<success_criteria>
1. A user can read `docs/install/<their-os>.md` start-to-finish and reach a working app. The validator gate guarantees the install commands inside still match `desktop-prod.sh` at every merge.
2. A user who hits a known error class sees an "Open docs for this error" button in the React error boundary; the button opens the right docs anchor in their default browser via Tauri shell.open.
3. Settings → API Keys panel shows the live state of all 3 token sources with an Active badge, and Save/Clear actions round-trip to the Plan 01-01 endpoints.
4. CI fails on a PR that edits `docs/install/macos.md` install commands without updating `scripts/desktop-prod.sh` to match.
5. README install section is ≤ 20 lines (was ~600); per-OS install pages own the details.
6. `backend/core/links.py` is the single source of truth for the project repo URL; both the Python map and the TS map use it (TS via a hardcoded mirror with a sentinel test guarding drift).
</success_criteria>

<output>
Create `.planning/phases/01-install-token-persistence-docs-scaffolding-error-ux/01-02-SUMMARY.md`. Include:
- The exact list of `<!-- validate -->`-tagged code blocks per docs file (so Plan 01-03 knows what changes to `desktop-prod.sh` will trigger docs revalidation)
- Confirmation of the 4-class taxonomy keys (locked here; Phase 5 bug reporter consumes this taxonomy)
- The decision on vitest setup (was it already configured, or did this task add it?)
- Any anchor IDs created that differ from the heading text (e.g. if GitHub's anchor algorithm produced something unexpected)
</output>
